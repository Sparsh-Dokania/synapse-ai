name: "synapse-ai-build"

services:
  db:
    image: "postgres:latest"
    environment:
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "postgres"
      POSTGRES_DB: "synapseaibuild"
    ports:
      - "5432:5432"
    restart: always
